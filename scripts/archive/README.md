Archived scripts and wrappers that are retained for history but are not part of the maintained operational surface.

Use canonical maintained scripts from:

- `scripts/slurm/`
- `scripts/dev/`
- `scripts/release/`

Do not submit new jobs from archived shell wrappers.
